
  become the superuser, then type:
  # make install

  By default cpuspeedy will be installed with the '/usr/local'
  prefix. You can change this by passing the PREFIX to make. 
  Eg:
  
  # make PREFIX=/usr install

